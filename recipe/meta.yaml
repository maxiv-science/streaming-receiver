package:
  name: streaming-receiver
  version: 0.0.1_dev

source:
  path: ..

build:
  number: 1
  script: {{ PYTHON }} -m pip install . -vv
  entry_points:
    - streaming-receiver = app.main:main
    
requirements:
  build:
    - {{ compiler('c') }}

  host:
    - python
    - setuptools-scm
    - cython
    - numpy
    
  run:
    - python
    - numpy
    - h5py
    - maxiv-bitshuffle
    - pyyaml
